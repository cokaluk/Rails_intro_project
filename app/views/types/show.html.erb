<div class="flex flex-col items-center mx-auto">
     <h1 class="mb-5 font-bold text-2xl"><%= @type.name%></h1>

     <h2 class="my-5 font-bold text-xl">Pokemon</h2>
     <% @type.pokemons.each do |pokemon| %>
          <p><%= pokemon.name %></p>
     <% end %>
</div>